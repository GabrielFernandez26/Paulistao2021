let doubleSpeed = function (velocity) {
	return velocity * 2;
}
let newVelocity = doubleSpeed(50)

document.write('O dobro da velocidade no argumento da função anônima '+ doubleSpeed+' é: '+newVelocity)